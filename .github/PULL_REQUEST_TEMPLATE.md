## Summary

<!-- What does this PR do? One or two sentences. -->

## Changes

<!-- Bullet list of notable changes. Be specific about modules/files touched. -->

-

## Testing

<!-- How was this tested? Check all that apply. -->

- [ ] Unit tests added / updated (`uv run pytest -m "not integration"`)
- [ ] Integration tests added / updated (`uv run pytest -m integration`)
- [ ] Manually tested against a real server
- [ ] No tests needed — docs / config / tooling only

## Checklist

- [ ] All tests pass locally (`uv run pytest`)
- [ ] Lint passes (`uv run ruff check src tests`)
- [ ] Types pass (`uv run mypy src`)
- [ ] `CHANGELOG.md` updated (if user-facing change)
- [ ] `README.md` updated (if public API or CLI changed)

## Notes for reviewer

<!-- Anything that needs extra attention, context on tricky decisions, known limitations. -->
